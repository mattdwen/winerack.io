@model winerack.Models.Varietal

@{
    ViewBag.Title = "Delete Varietal";
}

<div class="page-header">
    <h1>Delete</h1>
</div>

<div class="widget">
    <h3>Are you sure you want to delete this varietal?</h3>
    <dl class="dl-horizontal">
        <dt>@Html.DisplayNameFor(model => model.Name)</dt>
        <dd>@Html.DisplayFor(model => model.Name)</dd>

        <dt>@Html.DisplayNameFor(model => model.Style)</dt>
        <dd>@Html.DisplayFor(model => model.Style)</dd>
    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <button type="submit" class="btn btn-danger">Delete Varietal</button>
        @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-link" })
    }
</div>