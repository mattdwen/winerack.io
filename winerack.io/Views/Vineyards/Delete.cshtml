@model winerack.Models.Vineyard

@{
    ViewBag.Title = "Delete Vineyard";
}

<div class="page-header">
    <h1>Delete Vineyard</h1>
</div>

<div class="widget">
    <h3>Are you sure you want to delete this vineyard?</h3>

    <dl class="dl-horizontal">
        <dt>@Html.DisplayNameFor(model => model.Name)</dt>
        <dd>@Html.DisplayFor(model => model.Name)</dd>
    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()
        
        <button type="submit" class="btn btn-danger">Delete Vineyard</button>
        @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-link" })

    }
</div>
