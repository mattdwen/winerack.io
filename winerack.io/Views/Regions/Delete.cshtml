@model winerack.Models.Region

@{
    ViewBag.Title = "Delete Region";
}

<div class="page-header">
    <h2>Delete Region</h2>
</div>

<div class="widget">
    <h3>Are you sure you want to delete this region?</h3>

    <dl class="dl-horizontal">
        <dt>@Html.DisplayNameFor(model => model.Name)</dt>
        <dd>@Html.DisplayFor(model => model.Name)</dd>

        <dt>@Html.DisplayNameFor(model => model.Country)</dt>
        <dd>@Html.DisplayFor(model => model.Country)</dd>
    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <button type="submit" class="btn btn-danger">Delete Region</button>
        @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-link" })
    }
</div>
