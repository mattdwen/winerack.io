@using winerack.Helpers
<nav class="navbar navbar-full navbar-dark app-navbar bg-primary" role="navigation">
  <div class="container">
    <button class="navbar-toggler hidden-md-up pull-xs-right" type="button" data-toggle="collapse" data-target="#navMobile">
      <i class="fa fa-bars"></i>
    </button>

    <a class="navbar-brand" href="/">
      <i class="wine-corkscrew"></i>
      <span>wine<strong>rack</strong></span>
    </a>

    <div class="hidden-sm-down" id="navbarMain">

      <ul class="nav navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="/">Home</a>
        </li>

        @if (User.IsInRole(MvcApplication.ADMINISTRATOR_GROUP))
        {
          <li class="nav-item dropdown">
            <a class="nav-link" href="/Admin">Administrators</a>
          </li>
        }
      </ul>

      <ul class="nav navbar-nav pull-xs-right">
        <li class="dropdown">
          <button class="btn btn-link navbar-avatar" data-toggle="dropdown">
            <img class="media-object img-circle" src="@ExtensionMethods.ProfileImageUrl("sq_sm")" height="35" width="35"/>
          </button>
          <div class="dropdown-menu dropdown-menu-right">
            <a class="dropdown-item" href="/Account/Settings">Settings</a>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="javascript:document.getElementById('logoutForm').submit()">Sign out</a>
          </div>
        </li>
      </ul>

    </div>

    <div class="collapse navbar-toggleable-sm hidden-md-up" id="navMobile">
      <ul class="nav navbar-nav">
        @if (User.IsInRole(MvcApplication.ADMINISTRATOR_GROUP))
        {
          <li><a class="nav-link" href="/Admin">Administrators</a></li>
        }
        <li><a class="nav-link" href="/Account/Settings">Settings</a></li>
        <li><a class="nav-link" href="javascript:document.getElementById('logoutForm').submit()">Sign out</a></li>
      </ul>
    </div>

  </div>

</nav>

<!--<img class="media-object img-circle" src="@ExtensionMethods.ProfileImageUrl("sq_sm")" width="20"/>-->

@using (Html.BeginForm("SignOut", "Account", FormMethod.Post, new { id = "logoutForm" }))
{
  @Html.AntiForgeryToken()
}

